<%= render "partials/banner", header: "Fuck Yo Pets, Take A Holiday"%>

<h1>These are the pet nannies close to you!</h1>
    <div class="container">
      <div class="cards">
        <% @pet_nannies.each do |nanny| %>
        <div class="card">
          <%= nanny.user.name %>
        </div>
        <% end %>
      </div>
    </div>

<%= link_to "Add a new pet", new_pet_path, class: "nav-link" %>
